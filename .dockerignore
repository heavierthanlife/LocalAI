# ── Python ──
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/

# ── Virtual environments ──
venv/
.venv/
env/
.env/
.env

# ── IDE / Editor ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── Git ──
.git/
.gitignore
.gitattributes

# ── Data (volume-mounted) ──
data/
logs/

# ── Large local caches / agent dirs (never bake into image) ──
local_cache/
company_kb_files/
knowledge_lab_files/
.opencode/
.remember/
.playwright-mcp/
tools/
*.tar.gz

# ── Certificates (volume-mounted) ──
cert/
# cert/ is mounted as a volume, keep cert/ in .dockerignore to avoid
# baking certs into the image. Copy cert/ to the server separately.

# ── Documentation ──
*.md
!README.md

# ── Tests ──
tests/

# ── OS junk ──
.DS_Store
Thumbs.db
desktop.ini

# ── Docker files ──
Dockerfile
docker-compose.yml
.dockerignore

# ── Node modules (if any) ──
node_modules/

# ── Coverage ──
.coverage
htmlcov/
.pytest_cache/

# ── Misc ──
*.log
msedgedriver.exe
