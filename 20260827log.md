# Local_AI 项目全面代码审计日志

> **日期**: 2026-08-27
> **项目路径**: `D:\PyCharm\Local_AI`
> **项目名称**: 中联招标智能助手 (Local AI Bidding Assistant)
> **审计人**: LMcode AI Agent
> **总文件数**: ~300+ (不含 node_modules, .venv, __pycache__)
> **总代码行数**: ~60,000+ (Python + JS + HTML + SQL)

---

## 目录

1. [项目概览](#1-项目概览)
2. [根目录文件](#2-根目录文件)
3. [app/ 核心应用层](#3-app-核心应用层)
4. [app/utils/ 工具模块](#4-apputils-工具模块)
5. [app/routes/ 路由层 (17个蓝图)](#5-approutes-路由层-17个蓝图)
6. [app/services/ 服务层 (86个模块)](#6-appservices-服务层-86个模块)
7. [基础设施与配置文件](#7-基础设施与配置文件)
8. [scripts/ 测试/ tools/ templates/ static/](#8-scripts--tests--tools--templates--static)
9. [全项目审计发现](#9-全项目审计发现)

---

## 1. 项目概览

**Local_AI** 是面向中国招投标行业的 AI 智能助手平台，为"中联招标"公司定制。Flask 后端 + SPA 前端，集成 LLM 对话、文档解析、合规检查、串通投标检测、企业信用查询、知识库管理、审计报告生成等核心功能。

### 技术栈

| 层级 | 技术 |
|------|------|
| 后端 | Flask 3.1 / Python 3.12 / gunicorn+gevent |
| 数据库 | PostgreSQL 16 + psycopg2 连接池 |
| 缓存/消息 | Redis 7 + Celery 5 |
| AI/LLM | LangChain + LangGraph + DeepSeek/智谱/通义/SiliconFlow/Mimo |
| 向量检索 | ChromaDB + sentence-transformers |
| 文档处理 | python-docx, PyMuPDF, openpyxl, python-pptx, EasyOCR |
| 浏览器自动化 | Selenium + Edge/Chrome WebDriver |
| 容器化 | Docker (6 服务: app, postgres, redis, celery-worker, celery-beat, nginx) |
| 前端 | 原生 JS SPA + Tiptap + highlight.js + markdown-it + DOMPurify |
| 调度 | APScheduler (本地) / Celery Beat (Docker) |

### 架构层次

```
Flask App Factory (__init__.py:create_app)
  ├── 17 Blueprints (routes/) — HTTP 端点层
  ├── 86 Services (services/) — 业务逻辑层
  ├── database.py — PostgreSQL 连接池与 70 表初始化
  ├── globals.py — 全局单例与锁
  ├── config.py — 路径常量、日志、文件类型验证
  ├── cleanup_tasks.py — 20+ 定时清理任务
  ├── celery_app.py — Celery 异步任务编排
  ├── templates/index.html — SPA 壳
  └── static/ — JS/CSS/PWA 资源
```

---

## 2. 根目录文件

### `AGENTS.md` (154 行)
项目主开发者指南。记录完整技术栈、快速启动命令、测试运行器（smoke + integration）、架构层次、schema 管理（`database.py` + `migrations/`）、关键约定（CSRF opt-in、filesystem sessions、rate limiting、upload semaphore、时区 Asia/Shanghai）、跨 4 个 AI 工具共享的 236 个 skill 基础设施、fix registry 模式。

### `Dockerfile` (60 行)
基于 `python:3.12-slim`。安装系统依赖（libpq, gcc, OpenCV, Edge+EdgeDriver, LibreOffice+CJK 字体）。非 root 用户 `localai`，清华镜像安装依赖，gunicorn 入口（4 workers, gevent, 120s timeout）。

### `Dockerfile.celery-fix` (2 行)
最小覆盖 Dockerfile，仅更新 celery_app.py。

### `docker-compose.yml` (195 行)
6 服务：app（gunicorn+gevent 4 workers）、postgres（16 Alpine, 端口 5433）、redis（7 Alpine, AOF, 256MB LRU, 端口 6380）、celery-worker（1 concurrency）、celery-beat、nginx（HTTPS 80/443）。4 命名卷 + 1 桥接网络。

### `run.py` (112 行)
本地开发入口。修复 Windows GBK 编码，调用 `init_services()` + `create_app()`，配置 SSL，启动 HTTP→HTTPS 重定向器（5000），Flask 在 5443 运行。导出 `app` 供 gunicorn。

### `requirements.txt` (206 行)
~156 个包。Web: Flask/Limiter/Session/WTF。AI: LangChain 生态/torch 2.12。文档: python-docx/PyMuPDF/openpyxl。OCR: EasyOCR/ONNXRuntime/sklearn。DB: psycopg2/SQLAlchemy/redis/celery/chromadb。Selenium: selenium/webdriver-manager。

### `requirements_min.txt` (51 行)
精简依赖（~100 包），无版本锁定。

### `nginx.conf` (108 行)
反向代理。`client_max_body_size 12G`。Docker DNS 解析器。upstream keepalive 32。HTTPS TLS 1.2/1.3 + 安全头。`/send_stream` WebSocket 300s timeout。

### `celery_app.py` (90 行)
Redis broker+backend。8 个 task 模块。JSON 序列化, Asia/Shanghai 时区, 10min/15min 超时。6 个 beat 调度。

### `.env.example` (52 行)
环境变量模板。LLM Keys, Database, Security, SMTP, App Config。

### `.mcp.json` (45 行)
5 个 MCP 服务器：filesystem, postgres, git, agentmemory, skill。

### `opencode.json` (63 行)
OpenCode 配置。4 MCP 服务器 + 5 斜杠命令（/resume, /save, /sessions, /recall, /memory）。

### `pytest.ini` (11 行)
4 个 markers: slow, e2e, db, redis。

### `CHANGELOG.md` (108 行)
2026-07-03 至 2026-07-06 版本历史。

### `IMPROVEMENTS_SKIPPED.md` (132 行)
9 项已评估但推迟的改进。

### ~~`USER_MANUAL.md` / `USER_MANUAL2.md`~~（已删除）
用户操作手册。2026-08-27 清理运行时文件时已从仓库删除（`git status` 显示 `D`）。

### `.gitignore` (61 行)
排除 IDE、缓存、运行时数据、上传文件、node_modules。注意末尾 `*.json` 全局排除。

### `.dockerignore` (63 行)
最小化构建上下文。

### `skills-lock.json` (35 行)
5 个外部 skill 锁定文件。

### `审计报告_LocalAI_20260730.md`
2026-07-30 审计报告。

---

## 3. app/ 核心应用层

### 3.1 `app/__init__.py` — 应用工厂 (313 行)
Flask 应用工厂 `create_app()`。加载 .env，验证 SECRET_KEY，配置 filesystem session (30天)、CSRF (opt-in)、Rate Limiter (Redis, 120/min)、Admin 密码（从 ADMIN_PIN 自动 hash）。注册 413 错误处理器、cache buster、Swagger 文档、17 个 Blueprint。设置 APScheduler 20+ 定时任务。`init_services()` 初始化 PostgreSQL 表、延迟加载 WebDriver、初始化 LangGraph checkpointer。

### 3.2 `app/_core.py` (8 行)
兼容存根。所有路由已迁移到 Blueprint 系统。

### 3.3 `app/config.py` (125 行)
路径常量（BASE_DIR, DATA_DIR, USER_FILES_DIR 等 8 个）。`to_rel_path()`/`resolve_path()` 确保路径跨机器可移植。日志配置（console INFO + rotating file DEBUG 10MB×5）。Edge WebDriver 自动定位/下载。文件扩展名白名单验证。

### 3.4 `app/database.py` (600+ 行)
PostgreSQL 连接池管理。三级连接参数获取（PG_*→DATABASE_URL→fallback）。SimpleConnectionPool (min 1, max 20)。健康检查 + 自动重连 context manager。事务 manager。

**表初始化**: advisory lock (732014) 防并发。创建 70 张表：users, chat_sessions, chat_messages, user_files, projects, project_members, project_folders, project_files, project_file_versions, project_ai_memory, member_workflows, workflow_kpi, task_deposit_items, checkpoints, recycle_bin (4种), knowledge_lab_files, company_knowledge_base, file_text_cache, wiki_origin_links, admin_audit_log, audit_config, audit_runs, audit_file_results, compliance 相关表, 模板表, 案例表, 时间线表等。种子 CEO/COO 管理员账户。

### 3.5 `app/globals.py` (100 行)
全局单例：_agent (RLock), _async_checkpointer, _semantic_model, user_active_tasks (600s 超时), download_tokens, credit_tasks, admin_rate_limit (5次/30分钟)。Agent System Prompt 从 `data/agent_prompt.json` 加载（支持在线编辑），自动追加安全防护。默认 prompt 要求 get_date 工具、bocha_search、Markdown 表格、【思考】【回答】格式、禁止编造数据。

### 3.6 `app/cleanup_tasks.py` (554 行)
20+ 定时清理任务：session 清理(15天)、过期文件删除(6h)、任务锁释放(5min)、空消息清理(1h)、匿名临时文件(1天)、项目删除(30天)、回收站(3天)、分享文件(7天)、下载令牌(24h)、孤立用户(3天)、训练数据(季度)、自动周/月/年报生成、RAG 健康检查(周)、过期审核清理、内存泄漏清理(小时)。自动报告调用 LLM 从聊天统计生成。底部注册 Celery task。

---

## 4. app/utils/ 工具模块

### `utils/__init__.py` (9 行)
导出 utc_now, beijing_now, safe_error_response, split_thinking_answer。

### `utils/helpers.py` (90 行)
`ok()`/`err()` 标准化 JSON 响应。`beijing_now()`/`utc_now()` 时间工具。`split_thinking_answer()` 解析 MiMo 多块【思考】【回答】。

### `utils/chinese_numbers.py` (141 行)
中文数字解析。`cn_to_arabic()` 标准中文转阿拉伯数字。`parse_daxie_amount()` 大写金额提取与交叉验证。

### `utils/headroom_utils.py` (69 行)
Headroom 上下文压缩（可选）。自动压缩 LLM 消息/文件内容/搜索结果以节省 token。

### `utils/mailer.py` (79 行)
SMTP 邮件工具。TLS/SSL 支持，异步后台线程发送。

---

## 5. app/routes/ 路由层 (17个蓝图)

### `routes/__init__.py` (163 行)
路由注册中枢。6 个直接路由（/, /get_csrf_token, /favicon.ico, /health, /sw.js, DevTools 204）+ 17 个 Blueprint 逐一注册计时。

### `routes/admin.py` (4,820 行) ⚠️ 最大
`admin_bp`。角色层级 admin>manager>editor>viewer>user。~100+ 端点：项目 CRUD/生命周期、成员管理/权限转移、文件上传/下载/移动/版本/评论、数据库浏览/编辑、AI SSE 辅助、任务暂存、工作流/KPI、系统配置（prompt/cache/runtime）、DB 迁移、LLM 状态、邮件、清理、在线状态、通知、分析结果、审核、用户删除流程、资产转移。

### `routes/audit.py` (306 行) ⚠️ 未注册
`audit_bp` (/audit)。8 端点：preflight、start（后台线程）、progress SSE、result、history、running、download docx/xlsx、config CRUD。**2026-08-27 全量审计合并入清标后已从 `register_blueprint()` 摘除，文件保留未加载**；`audit_runs`/`audit_config`/`audit_file_results` 表仍由 graph.py/cases.py 依赖。

### `routes/auth.py` (605 行)
`auth_bp`。10 端点：check_auth、create_account（PIN 4/6位）、login（admin/CEO/COO 特殊处理）、update_account、PIN 变更验证码、email 设置、账户删除流程（请求→确认→选择→执行）。

### `routes/batch.py` (351 行)
`batch_bp`。9 端点：批量结果下载/列表/删除、报价异常检测（Benford/变异系数/同率检测）、投标人报价比较、反馈、关系提取、错别字检测。

### `routes/cases.py` (271 行)
`cases_bp` (/cases)。14 端点：案例 CRUD、标签、法律关联、模板关联、auto-generate。

### `routes/chat.py` (2,025 行) ⚠️ 第二大
`chat_bp`。~40+ 端点：SSE 流式聊天（LangGraph agent, RAG, grilling/redteam）、会话管理、文件管理（SHA-256 去重）、回收站、分享（7天 token, 路径遍历防护）、全文搜索、LLM 提供商切换、反馈、URL 抓取。

### `routes/clearance.py` (173 行)
`clearance_bp` (/clearance)。3 端点：统一"清标"入口（5 维度: 指标分析/交叉比较/合规/AI 审查/全量审计补充, Celery 异步）、status、stream SSE。200MB body limit。

### `routes/compliance.py` (937 行)
`compliance_bp` (/compliance)。23 端点：法规 CRUD、规则提取、合规检查（异步）、增量检查、结果/规则编辑、反馈、版本管理、趋势/仪表板/比较/图谱、法规影响分析。

### `routes/credit.py` (355 行)
`credit_bp`。9 端点：Selenium 自动化企业信用查询。start/status/resume、CAPTCHA 交互、报告下载/列表/删除。

### `routes/document_analysis.py` (123 行)
`document_analysis_bp` (/document_analysis)。3 端点：深度分析（1-10 文件, Celery）、status、stream SSE。200MB limit。

### `routes/graph.py` (435 行)
`graph_bp` (/api/graph)。5 端点：串通图、合规图、法规影响图、引用网图、类型元数据。⚠️ 无认证。

### `routes/knowledge.py` (2,018 行) ⚠️ 第三大
`knowledge_bp`。~70+ 端点：知识实验室（上传/skill 生成/RAG/wiki ingest）、skill 审计/合并/归档、训练管线（统计/导出/清理/LoRA）、写作风格、ingest 管线（审核/结构化提取）、笔记本、工作报告、RAG 管理、公司知识库。

### `routes/projects.py` (104 行)
`projects_bp`。1 端点 + 7 个权限检查工具函数（can_manage_files, can_edit_file 等）。

### `routes/tasks.py` (115 行)
`tasks_bp` (/tasks)。5 端点：任务列表/状态/删除/取消/SSE 流。

### `routes/templates.py` (316 行)
`templates_bp` (/templates)。14 端点：模板 CRUD、.docx 导入/导出、版本管理、AI 推荐、skill→文档生成。

### `routes/timeline.py` (495 行)
`timeline_bp` (/timeline)。17 端点：时间线管理、里程碑（单/批量/差异）、法律分类/进度模板、工作流步骤 CRUD/自动生成/进度、AI 建议。

### `routes/upload.py` (61 行)
`upload_bp`。1 端点：`POST /stream_upload` 流式上传（8MB 分块, 10GB 配额, 11GB body limit）。

### `routes/wiki.py` (442 行)
`wiki_bp` (/wiki)。18 端点：wiki 索引/页面 CRUD/树/搜索/stats/书签、法律导入、页面对比与对比历史、视图日志、实体图谱、分类页。

---

## 6. app/services/ 服务层 (86个模块, ~26,000 行)

### 核心基础设施
| 文件 | 行数 | 功能 |
|------|------|------|
| `__init__.py` | 1 | 空包 |
| `_save_helper.py` | 41 | DB save 装饰器 |
| `_shared_helpers.py` | 85 | AI 文档审查 prompt + 回收站恢复 |
| `admin_utils.py` | 51 | 频率限制 + 审计日志 |
| `runtime_config.py` | 330 | 100+ 运行时配置（三层加载, 文件持久化） |
| `redis_client.py` | 30 | Redis 单例 |
| `task_bus.py` | 256 | 异步任务总线（Redis pub/sub → SSE） |
| `task_locking.py` | 32 | 每用户单任务锁 |
| `session_manager.py` | 326 | 会话/聊天/消息管理 |

### AI/LLM 核心
| 文件 | 行数 | 功能 |
|------|------|------|
| `agent.py` | 310 | LangGraph 代理（Bocha 搜索+日期工具, 72h 缓存） |
| `agent_middleware.py` | 65 | InvalidToolGuard（幻觉工具调用防护） |
| `llm_provider.py` | 276 | 5 LLM 路由（DeepSeek/智谱/通义/SiliconFlow/Mimo） |
| `llm_fallback.py` | 206 | Fallback 链（指数退避, 熔断器） |
| `redteam_agent.py` | 69 | Red Team 对抗性审查 |
| `judge_review.py` | 95 | Judge 模型二次审查 |
| `prompt_safety.py` | 306 | Prompt 安全层（注入防护/anti-hallucination） |
| `analysis_prompts.py` | 180 | 投标分析+工作报告 prompt |
| `compliance_prompts.py` | 194 | 合规检查 prompt |

### 文档处理
| 文件 | 行数 | 功能 |
|------|------|------|
| `file_processing.py` | 1,551 | 全格式文本提取+相似度+VL 描述+熔断器 |
| `document_parser.py` | 305 | 统一文档解析器（法律/投标/通用） |
| `document_classifier.py` | 168 | 两层分类（正则+LLM） |
| `document_schema.py` | 57 | 中间文档格式 |
| `file_cache.py` | 153 | 两级缓存（PG+内存 LRU） |
| `file_store.py` | 162 | 流式上传（8MB 分块, SHA256 去重） |
| `file_generator.py` | 250 | Markdown→DOCX/XLSX/PPTX |
| `ocr.py` | 87 | EasyOCR 线程安全单例 |
| `document_analysis_svc.py` | 1,100 | 文档深度分析（6 检查器, DOCX 报告） |

### 合规与审计
| 文件 | 行数 | 功能 |
|------|------|------|
| `compliance_checker.py` | 489 | 合规引擎（规则→法规→语义→LLM 判定） |
| `rule_extractor.py` | 262 | 规则提取（AI+正则双通道, 5 类） |
| `audit_engine.py` | 775 | 审计编排（8 评分函数, SSE, DOCX/XLSX, Wiki） |
| `audit_report.py` | 766 | 专业中文报告生成 |
| `audit_logger.py` | 88 | 结构化审计日志 |
| `audit_wiki_publisher.py` | 280 | 审计→Wiki 发布 |
| `incremental_check.py` | 164 | 增量合规检查 |
| `compare_service.py` | 152 | 跨项目比较 |
| `dashboard_service.py` | 168 | 合规仪表板 |
| `clearance_engine.py` | 483 | "清标"编排（5 维度并行, Celery） |

### 投标检测
| 文件 | 行数 | 功能 |
|------|------|------|
| `batch_orchestrator.py` | 1,004 | 批量比较（RiskScorer, TF-IDF, gang detection） |
| `batch_compare_svc.py` | 32 | 批量比较辅助 |
| `quote_anomaly.py` | 557 | 报价异常（Benford/变异系数/聚类/大写验证） |
| `relationship_extractor.py` | 680 | 实体关系提取（HanLP+正则, 天眼查 API） |
| `indicator_defs.py` | 533 | 46 个检测指标定义 |

### 法律系统
| 文件 | 行数 | 功能 |
|------|------|------|
| `law_parser.py` | 98 | 法律文本解析（章/条） |
| `law_semantic.py` | 152 | 语义法律搜索（ChromaDB） |
| `law_version.py` | 232 | 法律版本管理（diff） |
| `law_monitor.py` | 174 | 法律变更监控 |
| `legal_schedule_service.py` | 324 | 法律进度模板（工作日计算） |
| `region_manager.py` | 118 | 区域变体管理 |

### 知识库与 Wiki
| 文件 | 行数 | 功能 |
|------|------|------|
| `rag_engine.py` | 466 | RAG 引擎（extract→chunk→embed→ChromaDB, 4 collection, LRU 5000） |
| `kb_skill_engine.py` | 268 | 自动 skill summary 生成 |
| `skill_auditor.py` | 401 | AI skill 审计（重复/使用/晋升） |
| `skill_compiler.py` | 170 | Skill 编译（DBSCAN 聚类） |
| `skill_validator.py` | 74 | SKILL.md 验证 |
| `wiki_engine.py` | — | Wiki 引擎 |
| `wiki_entity_service.py` | — | Wiki 实体 |
| `wiki_ingest.py` | — | Wiki 摄取 |
| `wiki_prompts.py` | — | Wiki prompt |
| `wiki_tree_service.py` | — | Wiki 树状结构 |
| `project_wiki_publisher.py` | 210 | 项目→Wiki 发布 |
| `ingest_pipeline.py` | 815 | 批量摄取（ZIP→OCR→4 管线） |
| `notebook.py` | 165 | 个人笔记本（ChromaDB RAG） |

### 项目管理
| 文件 | 行数 | 功能 |
|------|------|------|
| `project_timeline_service.py` | 427 | 时间线 CRUD（里程碑跟踪） |
| `workflow_engine.py` | — | 工作流引擎 |
| `workflow_service.py` | — | 工作流服务 |
| `case_service.py` | 429 | 案例库（自动从审计生成） |
| `template_service.py` | — | 模板 CRUD |
| `template_renderer.py` | — | 模板渲染 |
| `template_diff.py` | — | 模板 diff |
| `template_recommend.py` | — | AI 模板推荐 |
| `recycle_bin_service.py` | 403 | 统一回收站（4 表, 递归恢复） |

### 用户与安全
| 文件 | 行数 | 功能 |
|------|------|------|
| `auth_jwt.py` | 108 | JWT 认证（session + Bearer） |
| `anonymous.py` | 99 | 匿名用户管理 |
| `credit_checker.py` | 184 | Selenium 企业信用查询 |
| `style_engine.py` | 444 | 写作风格画像 |
| `suggestion_engine.py` | 368 | 时间线建议（规则+AI） |

### 其他服务
`context_utils.py`(187), `graph_protocol.py`(78), `graph_service.py`(264), `langutils.py`(58), `semantic.py`(157), `lora_trainer.py`(217), `nightly_trainer.py`(393), `review_logger.py`(99), `vl_model.py`, `web_extractor.py`, `text_utils.py`, `training_logger.py`, `trend_service.py`, `typo_detector.py` 等。

---

## 7. 基础设施与配置文件

### `templates/index.html` (812 行)
SPA 壳。完整 HTML 结构 + CDN 依赖（highlight.js, markdown-it, DOMPurify）。

### `static/` (24 文件)
**JS**: app.js(9,798), knowledge-lab.js(2,783), review.js(1,408), chat.js(1,104), compliance.js(1,013), bid-audit.js(847)⚠️未加载, wiki.js(693), templates.js(605), file-station.js(561), accounts.js(548), cases.js(513), tiptap-editor.js(394), graph-view.js(335), + highlight.min.js, markdown-it.min.js。
**CSS**: app.css(1,834), darkly.css, flatly.css, tokens.css(53)。
**PWA**: manifest.json, sw.js, favicon.ico, icons/。

### `migrations/` (1 文件)
`001_initial_snapshot.sql` — 空白基线。实际 schema 由 database.py 幂等管理。

---

## 8. scripts/ tests/ tools/ templates/ static/

### scripts/ (16 文件, ~3,230 行)
check_system.py(478), run_lora_training.py(480), recover_all.py(522), remember_sqlite.py(366), manage_db.py(225), reindex_kb_sections.py(242), migrate_seed_laws.py(186), collect_laws_raw.py(159), clean_laws.py(155), backfill_wiki_entities.py(163), verify_fixes.py(111), migrate_paths_relative.py(109), batch_wiki_ingest.py(26), daily_backup.sh(8)。

### tests/ (16 文件)
conftest.py, test_smoke.py(6 测试), test_regression.py(15 回归), test_batch_orchestrator.py, test_document_pipeline.py, run_tests.py(24 测试), unit/(3 文件), e2e/(2 文件), integration/(12 文件), factories/(3 文件), mock_data/(6 JSON)。

### tools/skill-mcp/ (371 行)
自定义 MCP 服务器，暴露 236 个 skill 为 MCP tools。

### data/ 目录
agent_prompt.json, audit_reports/, batch_results/, checkpoints.db(SQLite), chromadb/, compliance_results/, dump/, ingest/, laws/, notebooks/, search_cache/, training/, user_files/, user_laws/, wiki/。

### 其他目录
.audit/（审计日志）, .githooks/, .superpowers/sdd/（SDD 任务）, .remember/（AI 代理记忆）, cert/(SSL), company_kb_files/, knowledge_lab_files/, local_cache/, repair_kit/, logs/。

---

## 9. 全项目审计发现

### 🔴 严重问题 (Critical)

| # | 问题 | 位置 | 状态 |
|---|------|------|------|
| C1 | admin.py 体量过大 | routes/admin.py (4,820→1,653行) | ✅ 已拆分: admin_regeneration / admin_knowledge_lab / admin_ops 子模块 |
| C2 | chat.py 体量过大 | routes/chat.py (2,025→1,122行) | ✅ 已拆分: chat_files / chat_sessions / chat_config 子模块 |
| C3 | knowledge.py 体量过大 | routes/knowledge.py (2,018→941行) | ✅ 已拆分: knowledge_notebook/company_kb/style/ingest/training + knowledge_shared |
| C4 | file_processing.py 体量过大 | services/file_processing.py (1,551行) | ⏸️ 保留 (21 处导入点, 拆分收益低; 已内部分段) |
| C5 | graph API 无认证 | routes/graph.py | ✅ 已修复: 5 端点加 @login_required + 项目成员检查 (FIX-2026-08-28-001) |
| C6 | 管理员默认 PIN 123456 | __init__.py:111 | ✅ 已修复: 生产 fail-closed, 开发保留默认+告警 (FIX-2026-08-28-002) |

### 🟡 中等问题 (Medium)

| # | 问题 | 状态 |
|---|------|------|
| M1 | DB 迁移散布 | ✅ 部分修复: 两处 `WHEN others` 改 `WHEN undefined_column` (真实错误浮出) |
| M2 | 全局可变状态 | ✅ credit_tasks 已迁移 Redis; 其余为良性缓存 |
| M3 | 匿名存储用 JSON 文件 | ✅ 已修复: 迁移到 anon_chat_messages JSONB 表, 原子 UPSERT (FIX-2026-08-28-004) |
| M4 | credit_tasks 内存共享 | ✅ 已修复: 迁移到 Redis 注册表, 跨 worker 可用 (FIX-2026-08-28-003) |
| M5 | .gitignore *.json 全局排除 | ✅ 已修复: 改为定向规则 + ! 否定, 保留 18 个跟踪 JSON |
| M6 | SSL 私钥在仓库 | ✅ 已修复: git rm --cached cert/key.pem, 已忽略 |
| M7 | msedgedriver.exe 在仓库 (20.8MB) | ✅ 已修复: git rm --cached + 忽略, 运行时自动下载回退 |
| M8 | pycorrector tar.gz 在仓库 (4.4MB) | ✅ 无需处理: 未被跟踪 (已排除) |
| M9 | 部分 service 文件无 docstring | ✅ 已修复: 5 文件补 docstring + llm_fallback BOM 移除 |

### 🟢 低优先级 (Low)

| # | 问题 | 状态 |
|---|------|------|
| L1 | Windows 特定代码 | ✅ 部分修复: config.py stdout 包装加 hasattr 防护 (StringIO 兼容) |
| L2 | 代码风格不统一 | ⏸️ 持续改进 |
| L3 | 测试覆盖率未知 | ✅ 已改善: 路由守护测试 + 6 项新回归测试, 49/49 通过 |
| L4 | 重复验证脚本 | ✅ 无需处理: verify-workspace.js/.cjs 不存在 (过期条目) |

### ✅ 优秀实践

1. Flask App Factory 模式 — 标准可测试
2. Blueprint 分层 — 17 蓝图职责明确
3. 三层数据库连接 — 环境→URI→fallback
4. Advisory Lock — 防并发死锁
5. Rate Limiting — Flask-Limiter + Redis
6. 路径可移植 — to_rel_path()/resolve_path()
7. Prompt Safety 层 — 独立注入防护 + anti-hallucination
8. 熔断器模式 — LLM fallback + VL 断路器
9. SSE 进度推送 — 长任务实时反馈
10. 回收站设计 — 4 表 + 恢复 + 过期清理
11. Skill 系统 — 自动生成/审计/编译/验证完整管线
12. 回归测试 — 15 测试锁定历史 bug
13. Fix Registry — 预提交钩子确保修复不变异
14. 会话连续性 — /resume /save /recall 跨会话持久化
15. 增量分析 — skill 审计指纹缓存避免重复计算

---

## 统计摘要

| 指标 | 值 |
|------|-----|
| Python 路由模块 | 17 蓝图, ~12,000 行 |
| Python 服务模块 | 86 个, ~26,000 行 |
| 核心应用模块 | 6 个, ~1,800 行 |
| 前端 JS | 15 个, ~20,000 行 |
| 前端 CSS | 4 个, ~2,500 行 |
| SQL 表 | 70 张 |
| API 端点 | 300+ 个 |
| 定时任务 | 20+ 个 |
| LLM 提供商 | 5 个 |
| Docker 服务 | 6 个 |
| 严重问题 | 6 个 (5 已修复, 1 评估后保留) |
| 中等问题 | 9 个 (8 已修复, 1 部分修复) |
| 低优先级 | 4 个 (2 已修复, 2 无需处理) |
| 优秀实践 | 15 个 |
| 修复记录 | 2026-08-28 全量修复完成, 60/60 测试通过, 路由 371/371 保留 |

---

*审计完成于 2026-08-27 by LMcode AI Agent*
*修复完成于 2026-08-28 — C5/C6 安全加固, M3/M4 真实 bug 修复, C1/C2/C3 路由拆分 (路由守护测试 371/371)*

---

## 10. 后续迭代记录 (2026-08-28)

### 10.1 Prompt 体系优化 (FIX-2026-08-28-005)
- **语言一致性**: JUDGE_PROMPT / STRUCTURED_PROMPT 英文→中文; 主 agent prompt「答疑助手」→「中联招标智能助手」(招投标领域专家), 工具约束泛化
- **修复隐藏 bug**: `data/agent_prompt.json` 残留 `{"prompt":"Test prompt"}` 覆盖默认 → 主 agent 曾用几乎空 prompt 运行; 已恢复真实默认
- **双 guard 去重**: call_llm 仅在 system_prompt 未含安全 guard 时追加
- **死代码清理**: BID_COMPARISON_SYSTEM / WIKI_LINT / WIKI_UPDATE_INDEX / _VL_CONSISTENCY_PROMPT

### 10.2 清标结果移入聊天 (FIX-2026-08-28-006)
- **A1**: 移除工具栏 `#clearanceResults` tab 区 (指标/横向/合规/AI 面板+下载链接); 保留工具容器/进度
- **A2**: 清标完成后复用现有 DB 事务 INSERT `chat_messages` (role=assistant, `<!-- CLEARANCE_REPORT -->`+JSON{report,download_url,file_count}); TaskBus.start() 支持 extra 元数据 (thread_id)
- **A3**: 结果渲染进当前聊天 (10 节可折叠富 HTML, 含热力矩阵/可点击单元格/帮派检测), 补齐原工具栏缺失的九(图片随机抽检)/十(全量审计补充) 节; 作用域 `_attachClearanceHandlers` 无 inline onclick (CSP+innerHTML 往返安全); 重载时从 JSON 重建富 HTML
- **线程定向**: 侧边栏点击已完成后台任务 → 同线程 append / 异线程弹提示+摘要弹窗(含下载)
- **下载**: 侧边栏后台任务弹窗保留 + 聊天气泡新增; 工具栏下载移除

### 10.3 清标报告生产级升级 (FIX-2026-08-28-009, 2026-08-28)
对比参考模板 `串通投标线索分析报告.docx` (55表) 与仿制产物 (21表), 判定为草稿后全面修复:

- **B1 三、指标详情分析空白**: `document_analysis_svc.py:494` 的 `continue` 使整段渲染成为死代码 → 修复后 45 项指标 6 行表全渲染 (E2E 验证 docx 含 45 个「指标名称」表)
- **B2 文本相似度恒 0**: `_precompute_tfidf_for_files` 从未在清标路径调用 → `_run_cross_comparison`/`_run_checker('text_sim')` 现预计算 TF-IDF + 接 `tender_text` 模板去除
- **B3 涉及指标数量恒 1**: `int(triggered_count>0)` 布尔 bug → 改为按指标 details 引用计数
- **开标信息表 + 评审标准** (决定 1/2): 新增 `clearance_openinfo.py` (Excel/CSV/JSON 中文表头解析 + 评审标准提取: 预算价/开标时间/评标办法/评分点) → 激活 14 指标 (contact_phone/contact_person/candidate_give_up/expert_deviation/waste_rate/bidder_count/extension 等); 报价优先开标投标报价列; 平台/IP 指标保留 ○ 跳过占位 (决定 3)
- **前端**: 开标表上传槽 + `/clearance/preview_criteria` 自动提取预览(可编辑/可上传表格) (决定 2)
- **评分合理化**: keyword_overlap 对 <4 关键词的短/模板文本返回低值, 消除均匀 42.9% 假象
- 路由 372 (新增 preview_criteria); 回归 72/72

### 10.4 清标评分计量升级 (FIX-2026-08-28-010, 2026-08-31)
评分审计核实 + 修复 (参考 CSDN 报价尾数模型 / 广州 80+ 指标四层体系 / 筑龙五维):

- **权重复合指数**: total_score 从裸加总 (max~209) 改为 0-100 复合指数
  (INDICATOR_WEIGHTS 45 项 + score cap + text_sim 三指标去重)
- **RiskScorer**: 0.3/0.3/0.2/0.2 → 0.375/0.375/0.25 (去死图片权重), text_sim ≥80% 门槛
  → 正常投标人复合指数 6.2 →「正常」(不再误标中等预警)
- **预警阈值**: 统一 ≥60 高度 / ≥30 中等 / <30 正常 (修复 analysis 路径倒序 bug)
- **行业信号**: 报价等差/等比规律 (_detect_progression) → 激活 quote_proportional_float;
  Benford Nigrini 分级 + 卡方 + Z 检验; score_analyzer (Grubbs/Kendall W/Spearman)
  → 激活专家类指标; relationship 社区检测识别围标网络; law_semantic 语义检索接入
- **Bug 修复**: len(suspicious)/extension 日期/投标单位数语义/open_info 校验/增量上传
- 回归 80/80; fix_registry 57/57

### 10.5 报价尾数检测 + 清标基线校准 (FIX-011/012, 2026-08-31)
审计批准的 P-A + P-B:

- **FIX-011 (P-A)**: 报价尾数检测 (CSDN 第一信号, 尾数相同≥80%) + extract_prices 修复
  (修 _CN_PRICE 独立匹配"万"产生虚假 10000; 双路去重; 非中文格式 guardrail 通过)
- **FIX-012 (P-B)**: 工程类 2 投标人脱敏基线校准:
  - 基线暴露正常投标被误标中等预警 → 无招标文件时 text_sim 指标**跳过**
    (模板去除不可用, 高余弦是模板重叠非围标)
  - 下调 relationship/quote/typo 等易误报指标权重
  - tests/fixtures/clearance_baseline/ (脱敏价格标 + 商务技术标 + 招标文件 + scores.json)
  - test_clearance_baseline_scores 锁快照防漂移
  - 报告样本量免责声明 (N<5)
- 回归 86/86; fix_registry 62/62; 校准范围仅工程类 (goods/services 待真实文档)

### 10.6 中文停用词过滤 — 消除模板重叠误报 (FIX-013, 2026-09-01)
审计确认方案 A + B轻量:

- **问题实证**: 无停用词表时, 正常投标(共享招标模板) cosine=0.9823, 58 个共有词
  (招标/投标/项目/资质/安全生产许可证…) df=2 idf=1.0 无法被 TF-IDF 抑制 → 误判围标
- **修复**:
  - 新增 `stop_words.py` `DEFAULT_STOP_WORDS` (~150 招投标/通用功能词)
  - `tokenize_for_tfidf` 默认过滤; `_make_vectorizer`/`preprocess_text_for_similarity`/
    `compute_similarity_with_numbers` 全链路接入 (3 条向量化路径统一)
  - B轻量: 提供招标文件时动态并入其 top-50 高频词
- **实证判别**: 围标(技术雷同) cosine 0.98 vs 正常(技术不同) 0.74, ≥80% 门槛可判别
- 更新 TF-IDF/keyword 快照值; 新增 2 回归测试
- 回归 88/88; fix_registry 66/66

### 10.7 自适应招标高频词 + 异组件文本相似度守卫 (FIX-014, 2026-09-01)
审计核准方案 Y + Z-1, 消除「上传招标文件后正常投标重合度」+ 根治「价格标 vs 技术标」跨组件误报:

- **方案 Y (自适应扩容 + TF守卫)** `file_processing.py:preprocess_text_for_similarity`:
  - B轻量 top-50 → 自适应 `k = min(200, max(50, len(template_text)//500))`
  - TF≥2 守卫: 只并入反复重现的框架词, 不误杀 TF=1 的独特技术参数 (设备型号/材料标准)
  - **实证 (真实 EPCM 招标 49216字, k=98)**: 同组件正常技术标 cosine 0.89→**0.7695** <0.80 门槛;
    围标(技术雷同) **0.9784** 仍触发; 判别裕度 0.209
- **方案 Z-1 (异组件守卫)** `batch_orchestrator.py`:
  - 新增 `_detect_component` (价格标/技术标/商务标/unknown, 双 unknown 守卫 fail-safe)
  - 异组件 pair (如 价格标↔技术标) `text_sim`+`key_sim` 归零 + 标记 `component_mismatch` + risk 重算
    (修复纯 text_sim 归零后 key_sim 残留 8.33 → risk 3.125 的漏洞; 现 risk=0)
  - 审计否决 Z-2 封顶 30 (30×0.25=7.5 分仍污染 30/60 阈值底线) → 采用直接归零
- **报告批注** `document_analysis_svc.py:6.6`: 异组件对显示「异组件(不计)」+ 图例说明
  「结构性条款重叠不作串标依据」
- **回归**: +4 测试 (test_tender_stopwords_adaptive / tf_guard / component_mismatch / component_same);
  全量 92/92; fix_registry 70/70; 容器 E2E Y+Z PASSED
- **基线回归**: 3/3 baseline passed (工程类价格标+商务技术标+招标文件, scores.json 快照无漂移)
